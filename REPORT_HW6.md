# ДЗ 6: Документация и отчеты

## 📋 Содержание

1. [Техническая документация](#1-техническая-документация)
2. [Публикация в Git Pages](#2-публикация-в-git-pages)
3. [Отчёты об экспериментах](#3-отчёты-об-экспериментах)

---

## 1. Техническая документация

### ✅ Создана документация с помощью MkDocs

Выбран **MkDocs Material** — современный фреймворк для документации с поддержкой:
- Тёмной/светлой темы
- Полнотекстового поиска
- Мермейд диаграмм
- Подсветки синтаксиса
- Адаптивного дизайна

**Файл конфигурации:** `mkdocs.yml`

```yaml
site_name: ResearchHub Documentation
theme:
  name: material
  language: ru
  features:
    - navigation.tabs
    - search.suggest
    - content.code.copy

plugins:
  - search
  - mkdocstrings
```

### ✅ Создано руководство по развертыванию

Документация включает подробные инструкции:

| Раздел | Путь | Описание |
|--------|------|----------|
| **Быстрый старт** | `docs/getting-started/quickstart.md` | Запуск за 5 минут |
| **Установка** | `docs/getting-started/installation.md` | Poetry, Docker |
| **Конфигурация** | `docs/getting-started/configuration.md` | params.yaml, Pydantic |
| **Docker** | `docs/deployment/docker.md` | Docker Compose setup |
| **DVC Pipeline** | `docs/deployment/dvc.md` | Версионирование данных |
| **MLflow Server** | `docs/deployment/mlflow.md` | Трекинг экспериментов |
| **ClearML** | `docs/deployment/clearml.md` | MLOps платформа |

### ✅ Настроена автоматическая генерация документации

Используется `mkdocstrings` для автоматической генерации API документации из docstrings:

```bash
# Локальная сборка
make docs-build

# Локальный сервер для разработки
make docs-serve
```

**Структура API документации:**

```
docs/api/
├── index.md           # Обзор API
├── mlflow-utils.md    # MLflowExperimentManager, MLflowModelRegistry
├── decorators.md      # @mlflow_track, @log_execution_time, etc.
└── features.md        # TF-IDF, текстовые признаки
```

### ✅ Созданы примеры использования

Каждый раздел документации содержит практические примеры:

**Пример использования декораторов:**
```python
from researchhub.decorators import mlflow_track

@mlflow_track(
    experiment_name="my_experiment",
    log_params=True,
    log_model=True
)
def train_model(X, y, **params):
    model = RandomForestClassifier(**params)
    model.fit(X, y)
    return model
```

**Пример запуска пайплайна:**
```bash
# Автоматический запуск через DVC
poetry run dvc repro

# Пошаговый запуск
poetry run python scripts/fetch_arxiv_data.py --max-results 100
poetry run python scripts/preprocess_data.py
poetry run python scripts/train_model.py
```

### Структура документации

```
docs/
├── index.md                        # Главная страница
├── getting-started/
│   ├── quickstart.md               # Быстрый старт
│   ├── installation.md             # Установка
│   └── configuration.md            # Конфигурация
├── user-guide/
│   ├── overview.md                 # Обзор системы
│   ├── data.md                     # Работа с данными
│   ├── training.md                 # Обучение моделей
│   └── experiments.md              # Эксперименты
├── api/
│   ├── index.md                    # API Reference
│   ├── mlflow-utils.md             # MLflow утилиты
│   ├── decorators.md               # Декораторы
│   └── features.md                 # Features API
├── deployment/
│   ├── docker.md                   # Docker развёртывание
│   ├── dvc.md                      # DVC Pipeline
│   ├── mlflow.md                   # MLflow Server
│   └── clearml.md                  # ClearML
├── reports/
│   ├── experiments.md              # Отчёт об экспериментах
│   └── metrics.md                  # Детальные метрики
└── stylesheets/
    └── extra.css                   # Кастомные стили
```

---

## 2. Публикация в Git Pages

### ✅ Настроен GitHub Actions для автоматической публикации

**Файл:** `.github/workflows/docs.yml`

```yaml
name: Deploy Documentation

on:
  push:
    branches: [main, master]
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: snok/install-poetry@v1
      - run: poetry install --with docs
      - run: poetry run mkdocs build --strict
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./site

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/deploy-pages@v4
```

### ✅ Создан сайт с документацией на Git Pages

После пуша в main документация автоматически:
1. Собирается с помощью MkDocs
2. Загружается как GitHub Pages artifact
3. Публикуется на `https://username.github.io/repository/`

### ✅ Настроено автоматическое обновление при изменениях

Workflow запускается при:
- Пуше в `main`/`master` с изменениями в `docs/` или `mkdocs.yml`
- Ручном запуске через `workflow_dispatch`

**Дополнительный CI workflow:** `.github/workflows/ci.yml`

Автоматически проверяет:
- Линтинг (Ruff)
- Форматирование
- Типы (MyPy)
- Безопасность (Bandit)
- Сборку документации

---

## 3. Отчёты об экспериментах

### ✅ Созданы отчёты об экспериментах в формате Markdown

**Файлы отчётов:**
- `docs/reports/experiments.md` — полный отчёт об экспериментах
- `docs/reports/metrics.md` — детальный анализ метрик
- `reports/experiment_report.md` — автогенерируемый отчёт

### ✅ Добавлены графики и визуализации результатов

Скрипт `scripts/generate_reports.py` создаёт:

| Визуализация | Описание |
|--------------|----------|
| `accuracy_comparison.png` | Сравнение точности по экспериментам |
| `algorithm_boxplot.png` | Boxplot распределения по алгоритмам |
| `metrics_heatmap.png` | Тепловая карта метрик |
| `time_comparison.png` | Сравнение времени обучения |

**Пример визуализации (текстовый):**

```
Accuracy по алгоритмам:

Random Forest     ████████████████████████████████████ 35%
SVM               █████████████████████████ 25%
Logistic Reg.     ████████████████████ 20%
```

### ✅ Созданы сравнительные таблицы экспериментов

**Сводная таблица результатов:**

| Алгоритм | Лучший эксперимент | Accuracy | F1-score | CV Accuracy |
|----------|-------------------|----------|----------|-------------|
| **Random Forest** | RF_more_features | **0.350** | 0.205 | 0.400 ± 0.031 |
| **SVM** | SVM_linear | 0.250 | 0.180 | 0.320 ± 0.045 |
| **Logistic Regression** | LR_baseline | 0.200 | 0.150 | 0.280 ± 0.052 |

**Детальные результаты Random Forest:**

| Эксперимент | n_estimators | max_depth | Accuracy | F1 |
|-------------|--------------|-----------|----------|-----|
| RF_baseline | 100 | 10 | 0.275 | 0.165 |
| RF_more_trees | 200 | 10 | 0.300 | 0.185 |
| RF_deeper | 100 | 20 | 0.275 | 0.170 |
| RF_more_features | 100 | 10 (10k features) | **0.350** | **0.205** |

### ✅ Настроена автоматическая генерация отчётов

**Скрипт:** `scripts/generate_reports.py`

```bash
# Генерация отчётов
make reports

# Или напрямую
poetry run python scripts/generate_reports.py --output-dir reports
```

**Функции скрипта:**
- Загрузка данных из MLflow или файлов экспериментов
- Создание визуализаций (matplotlib, seaborn)
- Генерация Markdown отчёта с таблицами
- Автоматическое определение лучших результатов

---

## 📦 Добавленные файлы

```
.
├── mkdocs.yml                          # Конфигурация MkDocs
├── .github/
│   └── workflows/
│       ├── docs.yml                    # Деплой документации
│       └── ci.yml                      # CI проверки
├── docs/
│   ├── index.md                        # Главная
│   ├── getting-started/
│   │   ├── quickstart.md
│   │   ├── installation.md
│   │   └── configuration.md
│   ├── user-guide/
│   │   ├── overview.md
│   │   ├── data.md
│   │   ├── training.md
│   │   └── experiments.md
│   ├── api/
│   │   ├── index.md
│   │   ├── mlflow-utils.md
│   │   ├── decorators.md
│   │   └── features.md
│   ├── deployment/
│   │   ├── docker.md
│   │   ├── dvc.md
│   │   ├── mlflow.md
│   │   └── clearml.md
│   ├── reports/
│   │   ├── experiments.md
│   │   └── metrics.md
│   └── stylesheets/
│       └── extra.css
└── scripts/
    └── generate_reports.py             # Автогенерация отчётов
```

---

## Воспроизводимость

### Локальная работа с документацией

```bash
# Установка зависимостей
poetry install --with docs

# Запуск локального сервера
make docs-serve
# Открыть http://localhost:8000

# Сборка статического сайта
make docs-build
# Результат в директории site/
```

### Генерация отчётов

```bash
# Генерация отчётов с визуализациями
make reports

# Отчёт будет создан в reports/experiment_report.md
# Графики в reports/figures/
```

### Публикация на GitHub Pages

1. Push изменений в `docs/` или `mkdocs.yml`
2. GitHub Actions автоматически:
   - Соберёт документацию
   - Опубликует на GitHub Pages
3. Документация доступна на `https://rvenie.github.io/repository/`

--